---
description: Clean up a rough thought and append it to the Parking Lot in CLAUDE.md
argument-hint: <rough thought, however messy>
---

Take the following rough thought and add it to the Parking Lot section of
`CLAUDE.md`:

$ARGUMENTS

Rules:

- Rewrite it as **one line**, in clear plain English. Fix grammar and phrasing.
- Keep the original meaning exactly. Do not add detail, scope, or opinions that
  were not there. If the thought is ambiguous, keep the ambiguity rather than
  resolving it — ask instead.
- Prefix with a target phase tag where the thought clearly belongs to one
  (`[Phase 3]`), or `[unsure]` where it does not.
- Append to the end of the Parking Lot list. Remove the `- (empty)` placeholder
  if it is still there.
- Do not touch any other part of `CLAUDE.md`.
- Do not start work on the thought. Parking it is the whole job.
- Reply with just the line that was added, nothing else.

If the thought is too vague to write a meaningful line, ask one short
clarifying question instead of guessing.
